export type ArchetypeVector = {
  orderChaos: number;          // -100 (질서) ~ +100 (혼돈)
  altruismSelf: number;        // -100 (이타) ~ +100 (사리)
  asceticHedon: number;        // -100 (금욕) ~ +100 (쾌락)
  knowledgeDestruction: number;// -100 (이해) ~ +100 (파괴)
};
